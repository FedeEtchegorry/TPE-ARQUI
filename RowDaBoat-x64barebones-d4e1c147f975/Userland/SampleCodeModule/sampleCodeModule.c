#include <userlib.h>
#include <shell.h>
#include <snake.h>

int main() {

    startSnake(1);
   /* tSnake mySnake;
    tApple myApple;
    spawnSnake(mySnake);
    spawnApple(myApple, mySnake);
    drawSnake(mySnake, myApple);*/

	return 0;
}